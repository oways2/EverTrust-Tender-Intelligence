import{NextResponse}from"next/server";import{collectAll}from"../../../lib/collect";export const runtime="nodejs";export const maxDuration=60;export async function POST(){const started=Date.now();const tenders=await collectAll();return NextResponse.json({ok:true,count:tenders.length,tenders,elapsedMs:Date.now()-started,generatedAt:new Date().toISOString()})}