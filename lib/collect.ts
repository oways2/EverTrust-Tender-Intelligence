import {collectSudanBid} from "./collectors/sudanbid";import {tenders as demo} from "./demo-data";import type {Tender} from "./types";export async function collectAll():Promise<Tender[]>{const live=await collectSudanBid().catch(()=>[]);const seen=new Set<string>();const merged:[...Tender[]]=[];for(const t of [...live,...demo]){const key=(t.url+"|"+t.title).toLowerCase();if(seen.has(key))continue;seen.add(key);merged.push(t)}return merged}